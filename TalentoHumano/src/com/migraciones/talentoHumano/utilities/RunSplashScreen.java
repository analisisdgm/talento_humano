package com.migraciones.talentoHumano.utilities;

import javax.swing.JWindow;

public class RunSplashScreen extends JWindow {
	private static final long serialVersionUID = -2973156876752089402L;
	SplashScreen splash;

	public RunSplashScreen() {
		// splash = new SplashScreen();
		// AccesoView ventana = new AccesoView();
		// this.add(splash, BorderLayout.CENTER);
		// this.setSize(splash.getWidth(), splash.getHeight());
		// this.setLocationRelativeTo(null);
		// setVisible(true);
		// splash.velocidadCarga();
		// this.dispose();
		// ventana.runAcceso();
	}
}
